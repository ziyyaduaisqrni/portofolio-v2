import { BackgroundEffect } from './BackgroundEffect';

/** Ambient cyan/blue light that gently shifts with page-scroll progress. */
export function AnimatedBackground() {
  return <BackgroundEffect />;
}
